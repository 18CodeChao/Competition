# v6 验证记录（2026-09-23）

分类：R01/R07任务实现修复、R07/R08策略优化、本地测试工具。详见 [TASK_WORKFLOWS_V6.md](../docs/TASK_WORKFLOWS_V6.md)。

## 测试与真实包入口

- `python -m unittest discover -s tests -v`：105项通过，比v5增加17项。
- 用户完整15条日志数据单独放在tests/fixtures中，预期独立指定为15条/6个世界遗产/9种类型/周口店遗址；没有进入运行模块或上传包。
- 真实本地HTTP API：HTTP200业务401、鉴权修正、业务400参数修正、两页全量数据，在一次沙盒脚本中完成；故意重复页会失败，不重复计数。
- 真实子进程执行参赛程序生成的脚本，只操作测试新建临时文件；部署应用名、端口、TOKEN均替换成合成值，核验非样例硬编码；检查器文件字节保持不变。
- CRLF shell：独立mock核对解释器、内存换行规范和`$0`保留；没有把Windows上的mock包装成真实Linux检查器测试。
- 覆盖错误/旧任务检查结果拒绝、自动TOKEN提交无需模型标志、严格String协议、全量证据保护统计、未知年代、只在结算成功后保存SOP。

`scripts/validate_v6_workflows.py` **解压本轮上传包**，从解压目录启动真实main.py，经根路径HTTP调用，官方沙盒由临时合成文件/本机API服务模拟。

| 合成用例 | 阵营 | 接取轮 | 提交轮 | 结算轮 | 答题LLM次数 | 沙盒次数 | 沙盒实测ms |
|---|---|---:|---:|---:|---:|---:|---:|
| B，omega应用/7654端口 | 左方 | 1 | 3 | 4 | 0 | 1 | 410.75 |
| A，测试城/4条记录/2页 | 右方 | 1 | 3 | 4 | 0 | 1 | 384.49 |

两例使用独立预期答案结算，任务错误为空；参赛目录没有新增文件，session只出现一次。
报告 `v6-workflows-http.json`，日志摘录 `v6-answer-log-example.txt`。此测试用例只验证任务链路，使用本地Game的合成任务，不等于线上A/B原题认证。

## 换边与留出回归

未修改战斗/移动策略，仅对任务求解进行修改，因此进行前两夜260轮的代表性回归，而非重复整个8夜防守调参。
开发种子23、留出种子1009、各左右一次，共1040轮。v5对手取本轮开始HEAD c9d41f4快照；原始报告 `v6-regression.json` 含实际源码哈希。

| 种子 | v6方 | v6分数 | v5分数 | v6基地血量 | v6执行失败 | v6最大响应ms |
|---|---|---:|---:|---:|---:|---:|
| 23 | 左上 | 1010 | 1025 | 1430 | 17 | 119.06 |
| 23 | 右下 | 1025 | 1010 | 1365 | 20 | 103.63 |
| 1009 | 左上 | 1012 | 1025 | 1400 | 32 | 95.96 |
| 1009 | 右下 | 1025 | 1012 | 1460 | 31 | 85.39 |

接口异常和传输错误均为0；同种子交换版本后各阵营行动/分数保持一致，未观察到此范围内的战斗回归。
这里的任务仍是简单结构化题，不能用900任务分证明真实A/B已通过，也不是完整比赛胜率。
实际碰撞等执行失败仍存在，不与接口异常混淆；真实选路/弹道等历史模拟缺口仍适用。

## 版本与边界

- 实测Python3.12.14，另做Python3.11语法解析；不是3.11.10实机验证。
- `git diff --check`；官方任务书/接口文档、四张图片、rules.py、run.sh对比本轮HEAD，保持原样。
- 上传包20个运行文件，只含agent、main.py、run.sh、requirements.txt；不含日志、测试样本、判题器或报告。
- 桌面来源日志、测试样本、当前代码、上传包、基准和HTTP报告哈希见 `v6-final-manifest.json`。不修改旧报告来伪装新版已验证。
- 新题型/规范/年代歧义走后备流程；未知合法空结果、超大/慢API不保证在短流程中完成。须由平台日志继续核验。
