# v5 验证记录（2026-09-23）

范围：中文日志、任务范围/接取时机、跨轮答案保留、占位答案拦截。不是官方任务全对认证。
规则依据、样本日志分析、启发式边界见 [LOG_TASK_V5.md](../docs/LOG_TASK_V5.md)。

## 自动化验证

- 最终 `python -m unittest discover -s tests -v`：88项通过（本版新增13项）。
- 新增独立预期：左右阵营首夜71/80/81及次夜201/210/211；两格任务点绑定、未知点禁止越界；受伤后范围内移动；移动时继续读题、治疗时保留答案下一轮提交。
- 日志：每角色独立背包，当前采集不伪报收益，失败/跳轮不归因；武器反馈归到操作者；完整长Prompt/命令/错误；多行工具数据不冒充回合标题。
- 旧任务 A checker token校验、错误/截断处理、任务SOP、炮手持续操控回归均通过；这里只验证合成用例，不验证线上题库。
- 实测 Python 3.12.14；24个参赛/测试Python文件通过3.11语法解析，不等于3.11.10实机运行。
- 官方任务书/接口文档、4张jpg、rules.py、run.sh与本轮开始的Git HEAD内容一致。`git diff --check`通过。

## 真实 HTTP 与日志

脚本 `scripts/validate_v5_http.py` 启动真实 `main.py` 子进程，在空临时工作目录测试，stdout在内存中采集，测试工具另存报告。

- 种子881、左右各130轮，总260轮。接口/网络异常为0；具体执行失败数见 `v5-http.json`，不将动作失败混为接口异常。
- 260个Round/Request/Response段，session出现1次；工作目录未新增任何文件。
- 最终stdout 4366行、229207字节，约223.8 KiB。增加了用户要求的完整Prompt等内容，体积是此测试用例的观测值，不是平台日志配额承诺。
- 标准错误仅有服务启动信息；未发生日志写入异常。样例 `v5-log-example.txt` 取自此真实HTTP测试。
- 中文日志直接人工阅读。现有结构化分析器需显式 `--log-format json`，不把中文日志解析为虚假的0轮统计。

## 代表性换边与留出集

开发种子23、留出种子907，各左右1040轮，合计4160轮。对手由本轮开始的HEAD 07bfb60原始源码打包为 `baseline-v4.zip`。
双方使用同一 observed 模拟环境，机器人路径/选敌仍是 v4 记录的本地假设；未修改战斗结算来提高成绩。
1040轮覆盖前8夜，complete=false，不作为完整胜率。

| 种子 | v5基地 | v5分数 | v4分数 | v5基地血量 | v5执行失败 | v5最大决策ms |
|---|---|---:|---:|---:|---:|---:|
| 23 | 左上 | 1883 | 2034 | 2220 | 40 | 175.50 |
| 23 | 右下 | 2039 | 1898 | 840 | 54 | 161.75 |
| 907 | 左上 | 1722 | 2048 | 0（764轮毁） | 91 | 167.02 |
| 907 | 右下 | 2038 | 1809 | 280 | 117 | 207.78 |

四组接口异常与传输错误均为0；两组较高、两组较低、一组基地被毁，不能宣称稳定提分。
任务分均900，来自模拟结构化题，并没有运行真实自进化 B 沙盒题库。
该基准运行中仅调整了 `agent/human_log.py` 的日志文案，将“动作成功”改为“动作合法（效果以本轮状态为准）”。
基准使用进程内策略，不加载日志模块，全部策略源码哈希与最终包一致；基准保留实际起始哈希，最终文案另经HTTP和88项测试验证。

## 交付与未覆盖范围

- `dist/submission-v5.zip`：19个运行文件，只含agent模块、main.py、run.sh、requirements.txt；run.sh保留LF和Unix可执行位，无日志/测试/模拟器。
- 源码、输入日志、基准、HTTP报告和包的哈希见 `v5-final-manifest.json`。旧版报告和包保留原样。
- 无真实自进化 B 完整任务/结果，不能确定字段级错因或保证全对；占位检查属启发式，合法空结果等特殊题型需要依题目确认。
- 无法保证夜间十轮后任务点绝对安全；范围内无安全格时不越界，角色仍可能死亡。
- 对手武器完整伤害、真实机器人选路/绕障等模拟缺口沿用历史说明，不能以本地测试替代平台回放。
