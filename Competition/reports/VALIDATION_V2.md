# v2 本地验证记录（2026-09-21）

## 版本与边界

这是本地实验结果，不是官方认证或正式比赛胜率。规则与假设见
[EXPERIENCE_V2.md](../docs/EXPERIENCE_V2.md) 和第一版 A01—A09。
最终代码/测试/官方来源哈希见 `v2-final-manifest.json`，各最终对战报告也包含相同源码哈希。
旧版对手为修改前冻结的 `baseline-v1.zip`，压缩包哈希登记在最终报告中。

`v2-development.json`、`v2-comparison.json`、`v2-holdout.json` 及对应开发回放属于中间实现，
不得当作最终版本证据。更早的 VALIDATION.md、final-manifest.json 和原始报告属于 v1，均保留。

实际执行 Python 3.12.14；目标 Python 3.11.10。22个源码文件通过 `ast.parse(feature_version=(3,11))`，
这只验证语法兼容，不能替代目标解释器和官方机器实测。代码只用标准库，没有新增第三方依赖。
官方两份文档、四张图片、agent/rules.py 和 run.sh 的哈希与 v1 清单一致。

## 回归与真实HTTP

- `python -m unittest discover -s tests -v`：47项通过、0失败。
- 新增案例覆盖镜像半圈墙/共享炮位/出口、空爆心、多弹重叠、单炮手轮射、夜间任务与避险、
  集中采石、升级回血、墙修复采购、保守囤矿、敌情过期、前八夜波次和日志读取。
- 真实子进程 `main.py <port> --log-dir reports/v2-http-logs` 连续HTTP运行130轮，
  服务异常0、网络错误0、参与方动作失败0；随后结束该子进程，没有遗留服务。
- HTTP日志复读130轮，损坏行0；最高处理耗时约28.97ms。
  [日志统计与第71轮地图](v2-http-analysis.txt)，机器结果见 `v2-final-http.json`。
- 在解压目录执行 `git diff --check`，因无 `.git` 返回129；随后对全部源码与修改文档执行
  `git -c core.autocrlf=false diff --no-index --check -- NUL <文件>`，无空白错误。

## 完整对战口径

开发种子1；最终留出种子303、404。每个种子左右换边，各1300轮，双方处于同一个 observed 情景。
第1—8夜数量来自用户经验，第9、10夜采用 hold8 假设；机器人AI、列内位置与任务题库仍为本地模型。
简单结构化任务使双方任务得分均为900，不能据此推断真实任务解题能力。
下面保留执行失败次数：动作失败并不等同接口异常，也不因比分较高而忽略。

## 仍未覆盖

- 第9、10夜真实波次与官方机器人选敌/位移、任务总量是否双方共享。
- 官方LLM、沙盒、完整自然语言题库及正式硬件的性能/内存限制。
- 多火箭搜索为有时间预算的近似算法；防守逃生和维修采购还可能浪费回合。
- 长局动作失败尚未逐一归因，下一步需要真实逐轮日志定位碰撞、目标消失与资源/路径冲突。
- 敌情记录和火力估计已完成；自动购买召唤令以平推对方的策略尚未实现。
- 第9、10夜增长情景已提供参数，本报告未把未执行的情景写作测试通过。

## 最终完整对战结果

| 种子 | v2基地 | v2得分 | v1得分 | v2基地剩余血量 | v2动作失败 | v2最高耗时ms |
|---|---|---:|---:|---:|---:|---:|
| 1 | left | 2538 | 1794 | 1500 | 33 | 168.72 |
| 1 | right | 2432 | 1476 | 1460 | 13 | 247.80 |
| 303 | left | 2515 | 1741 | 1300 | 38 | 269.27 |
| 303 | right | 2624.5 | 1864.5 | 1500 | 30 | 249.07 |
| 404 | left | 2492 | 2026 | 1500 | 36 | 1229.11 |
| 404 | right | 2512 | 1722 | 1500 | 25 | 848.74 |

本地6场均为v2胜出，基地均存活；平均分 2518.92 对 1770.58，相对提高 42.3%。
参与方接口/进程异常合计 0，动作执行失败每场 13—38 次。
参与方最高单轮决策耗时 1229.11ms；同机并行模拟与负载影响计时，不视为官方机器性能承诺。

原始报告：`v2-final-development.json`、`v2-final-holdout.json`。
