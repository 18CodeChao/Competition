# v3 验证记录（2026-09-22）

范围：用户平台反馈修复、协议与策略回归、已知前8夜的本地对照。不是官方认证或提分保证。
来源与实现缺口见 [PLATFORM_V3.md](../docs/PLATFORM_V3.md)。

## 最终代码验证

- `python -m unittest discover -s tests -v`：60项通过。包含13项平台反馈新增用例及既有47项。
- 新增测试独立指定两边炮位、y=15边界、对方机器人溅射排除、炮手近身受击仍开火、冷却守位、
  没有工人时不征用先锋、穿越风险区返岗、任务优先、成功沙盒结果直接提交、失败结果拒绝、
  错答修正、成功SOP晋升、日志分段/丢段、敌方全图火箭不使全图禁行、失败移动改路。
  单个用例可能覆盖多个场景。
- 实际运行解释器 Python 3.12.14，23个文件通过Python3.11语法解析；未宣称3.11.10实机验证。
- `git diff --check` 与 `git diff --cached --check` 通过。
- 起始11个Git合并冲突已备份并在工作区和索引解决，没有创建提交，也没有改动用户IDE目录。
- `dist/submission-v3.zip` 只含运行所需源码、run.sh和requirements.txt，不含本地判题器、历史日志或IDE目录。

## 正式入口与平台日志

实际启动 main.py 子进程，在空临时工作目录中以HTTP运行种子811，左右各130轮。
共260轮stdout记录、2080行、4,231,732字节；完整重组，无缺段；工作目录新增文件数0。
两侧服务异常与网络错误均0；本版左侧动作执行失败0次、右侧9次。
右侧失败主要是双方抢占相同移动格，新增暂避策略避免连续原路重复，但不能消除所有同时碰撞。
测试后已结束子进程。完整机器结果及源码哈希在 `v3-final-http.json`。

## 最终本地对比

开发种子17，留出种子607，均左右换边、每组1040轮，覆盖已知第1—8夜。
双方使用同一observed环境；对手是修复前归档的v2，报告登记压缩包SHA256。
1040轮不是完整比赛，所以complete=false、localWinner=null，不把中途高分计作胜场。

| 种子 | v3位置 | v3分数 | v2分数 | v3基地血量 | v3动作失败 | v3最高耗时ms |
|---|---|---:|---:|---:|---:|---:|
| 17 | 左上 | 1892 | 2069 | 1455 | 91 | 146.58 |
| 17 | 右下 | 2037.5 | 1996.5 | 1500 | 44 | 121.29 |
| 607 | 左上 | 1896 | 2096 | 1300 | 25 | 144.71 |
| 607 | 右下 | 2054 | 1990 | 1500 | 56 | 147.94 |

四组基地均存活、接口异常均0，分数两高两低；平均1969.875对2037.875，约低3.34%。
不能据此声称v3稳定提分。v2仍会攻击对方机器人，改变双方击杀分和防守压力；模拟还缺玩家炮火对人物/墙的结算，
因此上述分数不能替代平台验证，更不能为了本地分数恢复用户明确禁止的选靶或离岗行为。
双方任务分均900，仅来自简单结构化题库，不代表真实自进化题的解题率。
最终原始报告：`v3-final-known8.json`。

## 中间版本与待验证

`v3-development.json`、`v3-holdout.json` 为加入移动失败重规划之前的1300轮试验，使用第9、10夜hold8假设；
`v3-http.json` 同属此前版本。它们和旧v1/v2报告全部保留为历史证据，不计为最终源码验证。
最终版本以 `v3-final-manifest.json` 所列源码哈希为准。

仍需平台原始任务文本、LLM/沙盒响应、真实对局日志确认任务格式、敌方炮火结算及各类动作失败。
没有实测官方机器的资源上限、平台日志配额和第9、10夜真实波次。
