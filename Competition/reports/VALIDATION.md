# 第一版验证记录（2026-09-21）

## 范围与版本

规则基线：任务书v1.0 2026-09-09，接口v1.0，用户2026-09-21补充及docs/1.jpg—4.jpg。
分类与所有假设见 `../docs/IMPLEMENTATION.md`（R01—R08，A01—A09）。
本报告是本地工程证据，不是官方认证、官方难度或官方胜率。

- 目标：Python 3.11.10，仅标准库。
- 实际运行：Windows，Python **3.12.14**（Codex随附解释器）。系统PATH里的python是3.7.0，不能用于本项目。
- 所有新增Python文件通过Python3.11语法解析检查；**尚未在3.11.10解释器上运行验证**。
- 可运行解释器：`C:/Users/ROG/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe`。
- JSON报告逐份登记源码、规则、图片SHA256、种子、阵营、压力、延迟、异常、执行失败。
- `development.json` 为墙体策略调整之前的**旧版本证据**，包含一场第1295轮基地被毁；不可当作当前代码验证。
- 当前主要证据：`development-v2.json`、`holdout.json`、`stress.json`、`http-smoke.json`。以各自登记的hash确认对应版本。
- `final-manifest.json` 登记最终源码和文档hash。测试文件在基准启动后补充了用例，因此基准报告中的测试文件hash可能更早；参赛与判题执行代码hash单独核对。

## 单元与接口回归

命令：`python -m unittest discover -s tests -v`（实际使用上述3.12.14可执行文件）。
结果：**33个测试通过**，约1秒。

覆盖：

- 2×2基地原点、图片建造环、正文优先射程、八方向对角通行。
- 同点争抢、互换、链式阻塞、移动到已腾空格。
- 电磁10能量对6/20血机器人造成6/4伤害；加特林最近命中与90度锥角；双火箭中心40/邻格20。
- 火箭发射后3轮空窗；致死伤害回合末结算；眩晕5轮、炸弹100伤害。
- 共享金币、售卖所得当轮不可使用、三炮总上限、操控者互斥。
- 同轮尾矿每工人各得1、刷新不进建造区、错误区域建造失败不扣材料。
- 升级满血、错误券不消耗、151轮复活保留背包、敌方视野过滤。
- 任务速度奖励、最高部分通过率、离开/超时结算、刷新冷却。
- 同轮双方获取宝藏、合法失败献祭消耗、每日10次召唤及下一夜生效。
- 普通LLM每日3次与任务期间豁免、沙盒回传超时/截断/未配置。
- 五次异常停调、全程生存550分、单方基地毁不提前终止。
- 真实HTTP UTF-8、重复请求幂等、畸形输入兜底、官方request.txt兼容。
- 注入模型回复→executeCmd→命令结果→模型答案→submitAnswer任务闭环。
- 私有_demo字段不影响输出，不修改请求对象。

新增眩晕测试最初错误地假设机器人一定选工人为目标；实际本地选敌在等距时优先基地。
已将预期改为独立检查解除眩晕后守方总血量减少20，未修改眩晕或攻击规则迎合测试。

## 当前版本：基础压力对Demo

所有半场均跑满1300轮，参赛程序零异常、无传输错误、基地存活。

| 集合 | 种子 | 参赛阵营 | 参赛分数 | 基地剩余HP | 执行失败 |
|---|---:|---|---:|---:|---:|
| 开发 | 1 | challenger | 1917.5 | 625 | 2 |
| 开发 | 1 | defender | 2027.5 | 4500 | 10 |
| 开发 | 7 | challenger | 1944 | 4500 | 11 |
| 开发 | 7 | defender | 1977 | 2990 | 9 |
| 留出 | 101 | challenger | 1947 | 2875 | 13 |
| 留出 | 101 | defender | 2025 | 4235 | 10 |
| 留出 | 202 | challenger | 1953 | 4500 | 6 |
| 留出 | 202 | defender | 2001.5 | 4500 | 9 |

这8半场参赛程序在本地模型中获胜。留出种子未用于策略参数调整。
参赛程序最高单轮耗时约103.34毫秒，各场P95约28.11—38.73毫秒；这是本机测量，不是官方机器性能保证。
执行失败独立统计，包含碰撞、闭矿采集或攻击无效；没有把这些算为队伍异常。

**重要限制：** 每场1200任务分来自8道公开结构化数值夹具题，每题1轮完成，不能外推真实自进化题得分。
Demo本身不做任务，故总分优势不代表在真实强对手下同样成立。

## 两倍本地波次压力

种子303，左右换边，零异常：

- challenger：1300轮，基地660HP，1897分，22次执行失败。
- defender：基地在1277轮被毁；对手基地在1030轮被毁，按先毁判负规则本地仍获胜；1885分，7次执行失败。

这说明高压力下仍有防守缺口，不能宣称全配置全程存活。pressure=2只是本地假设波次数量翻倍，不属于官方赛制。

## 启动、HTTP与回放

通过Git Bash真实执行 `bash run.sh <临时端口>`，用PYTHON环境变量指定3.12.14解释器。
模拟判题器向HTTP服务发送130轮请求，零异常、零传输错误、零执行失败，最高往返约45.81毫秒。
`http-replay.jsonl`保存每轮双方公开请求、响应、事件与结算摘要，约1.8MB。
该次只跑一昼夜，报告complete=false，不用于完整胜负结论。测试服务已停止。

## 同策略双队对抗

`selfplay.json`：种子404、基础压力、两半场换边，双方均运行本版Agent。
两场均1300轮、双方零异常、基地均存活；按阵营的得分为1919.5/1922.5，交换选手后两半场合计平局。
同种子与同策略得到一致游戏结果。两队争抢中立资源时分别出现38/36次执行失败，属于仍可优化的冲突处理。
最高单轮约113.70毫秒。完整当前版本共12个半场（8个对Demo基础场、2个压力场、2个自对弈场），均无参赛异常。

## 文件检查

- `bash -n run.sh`通过。
- 已尝试 `git diff --check`，目录不是Git仓库，无法进行仓库差异检查；没有擅自初始化Git仓库。
- 对新增源码、入口和说明文件执行 `git -c core.autocrlf=false diff --no-index --check -- NUL <文件>`，无空白错误。
- `.gitattributes`指定run.sh使用LF，避免未来Git换行转换破坏正式Linux启动。

## 未覆盖

真实Python3.11.10运行、官方机器资源限制、真实LLM与隔离沙盒执行、真实任务难度、官方机器人AI、弹道边界、击杀归属及完整胜负歧义仍未经过官方平台校验。
协议与本地执行覆盖全部12动作；策略暂未主动优化召唤令、炸弹、眩晕、拆墙、维修与丢弃。
收到真实日志后应新增独立回归，并保留此报告作为当前版本的历史证据。
